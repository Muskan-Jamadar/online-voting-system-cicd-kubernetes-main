apiVersion: v1
kind: Service

metadata:
  name: voting-app-service
  namespace: production

spec:
  selector:
    app: voting-app

  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080

  type: NodePort